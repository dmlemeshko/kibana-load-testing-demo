app {
  host = "http://localhost:5620"
  version = "8.0.0"
}

security {
  on = true
}

auth {
  username = "elastic"
  password = "changeme"
  loginPayload = """{"username":"elastic","password":"changeme"}"""
}